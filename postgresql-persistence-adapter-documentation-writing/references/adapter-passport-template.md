# Шаблон паспорта PostgreSQL-адаптера

Использовать обязательные заголовки общего скила в указанном порядке:

```markdown
# Паспорт адаптера PostgreSQL persistence

## Назначение

## Границы ответственности

## Контракты

|Сторона|Контракт|Владелец|Документ-источник|Роль адаптера|
|:---|:---|:---|:---|:---|

## Внешняя система

## Преобразования

|Внутреннее сведение|Строка или значение PostgreSQL|Правило преобразования|Отсутствие или ошибка|
|:---|:---|:---|:---|

## Гарантии взаимодействия

## Ошибочные исходы

|Ситуация PostgreSQL|Исход application-порта|Повтор допустим|Безопасный контекст|
|:---|:---|:---|:---|

## Конфигурация

## Совместимость и внешние объекты

Ссылка на `postgresql-persistence/current/README.md` и индекс change packages.

## Проверка интеграции

## Наблюдаемость

## Готовность

## Открытые вопросы
```

## Распределение PostgreSQL-деталей

- Unit of Work, commit/rollback, optimistic locking, batching, version history и
  outbox помещать в `Гарантии взаимодействия`.
- Mapping текущего состояния и восстановления помещать в `Преобразования`.
- Миграционную стратегию, владение схемой и offline migration помещать в
  `Совместимость и внешние объекты`.
- Полный состав таблиц и индексов в паспорт не копировать.
- Неприменимый обязательный раздел сохранять с краткой причиной.
- Дополнительный `##`-раздел разрешён только перед `## Готовность`.
- `## Открытые вопросы` всегда последний.
